version: 2

project_name: envsync

before:
  hooks:
    - go mod tidy

builds:
  - id: envsync
    main: .
    binary: envsync
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/envsync/envsync/cmd.Version={{.Version}}
      - -X github.com/envsync/envsync/cmd.GitCommit={{.ShortCommit}}
      - -X github.com/envsync/envsync/cmd.BuildDate={{.Date}}

archives:
  - id: default
    format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"

release:
  github:
    owner: dantwoashim
    name: Env_sync
  draft: false
  prerelease: auto
